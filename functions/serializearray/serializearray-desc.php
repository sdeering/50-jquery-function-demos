The .serializeArray() function can <strong>create an array of form fields and thier values</strong> in seconds! There are two examples, the first outputs all of the form fields and thier values. The second creates a URL string with the form fields and values appended to the form action ready to be sent.