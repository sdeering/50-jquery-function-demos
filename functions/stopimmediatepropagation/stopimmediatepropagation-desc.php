Nice function to <strong>stop all future bound events</strong>. The events will fire in the order they were bound and when it hits the .stopImmediatePropagation() function all further bound events are not fired.</p>
<p class="example">Demo: Preventing all future bound events from firing.