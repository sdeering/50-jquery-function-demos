The $.getJSON() function is an AJAX <strong>shorthand method for accessing JSON data files</strong>. JavaScript Object Notation (JSON) provides a great way to store and transfer data independantly and it works great with JavaScript (because it's essentially the same notation). There are a few reasons why you might choose <a href="http://www.jquery4u.com/json/json-vs-xml/" target="_blank">JSON over XML</a>.</p>

<p>For this demo you can <a target="_blank" href="view-source:http://jquery4u.com/function-demos/data/colors.json" class="viewjson">view the JSON file.</a> We can also use functions like $.parseJSON() and JSON.parse() from ECMAScript 5 that simplifies JSON parsing. If you're interested in JSON Parsing and Tree Recursion see <a href="http://www.jquery4u.com/tutorials/online-json-tree-viewer/" target="_blank">JSON Tree Viewer</a> which is very interesting.